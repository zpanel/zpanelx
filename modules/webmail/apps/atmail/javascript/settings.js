// Conditionally perform the rollovers in JavaScript 1.0
function swapImage(imageName, imageBase) {
document[imageName].src = 'imgs/' + imageBase + '.gif';
//document[LoginTypes].src = 'imgs/simple.gif';
}

